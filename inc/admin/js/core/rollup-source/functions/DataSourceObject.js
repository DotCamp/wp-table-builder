/**
 * Data source card object.
 *
 * @param {string} id unique id
 * @param {string} title title
 * @param {string} info information for source
 * @class
 */
function DataSourceObject(id, title, info) {
	this.id = id;
	this.title = title;
	this.info = info;
}

export default DataSourceObject;
